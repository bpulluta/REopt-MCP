{
  "name": "reopt-mcp",
  "version": "0.5.0",
  "description": "MCP server for interacting with NLR's REopt API",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "server": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0-beta.3",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
